{
  "name": "ai-voice-studio",
  "displayName": "AI Voice Studio",
  "description": "Read text aloud in VS Code with OpenAI, MiniMax, MiMo, and Google Gemini TTS — voices, voice clone, emotion tags, and director mode from the sidebar.",
  "version": "0.6.1",
  "publisher": "xianwei-zhang",
  "license": "MIT",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/xwzhangSZU/vscode-ai-voice-studio.git"
  },
  "bugs": {
    "url": "https://github.com/xwzhangSZU/vscode-ai-voice-studio/issues"
  },
  "homepage": "https://github.com/xwzhangSZU/vscode-ai-voice-studio#readme",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "tts",
    "text-to-speech",
    "speech",
    "voice",
    "openai",
    "minimax",
    "mimo",
    "gemini",
    "voice clone",
    "voice design"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onView:aiVoiceStudio.studio",
    "onCommand:aiVoiceStudio.quickRead",
    "onCommand:aiVoiceStudio.stop",
    "onCommand:aiVoiceStudio.setApiKey",
    "onCommand:aiVoiceStudio.clearApiKey",
    "onCommand:aiVoiceStudio.focusView"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "aiVoiceStudio",
          "title": "AI Voice Studio",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "aiVoiceStudio": [
        {
          "type": "webview",
          "id": "aiVoiceStudio.studio",
          "name": "Voice Studio"
        }
      ]
    },
    "commands": [
      {
        "command": "aiVoiceStudio.quickRead",
        "title": "Read Selection (or Clipboard)",
        "category": "AI Voice Studio"
      },
      {
        "command": "aiVoiceStudio.stop",
        "title": "Stop Reading",
        "category": "AI Voice Studio"
      },
      {
        "command": "aiVoiceStudio.setApiKey",
        "title": "Set API Key…",
        "category": "AI Voice Studio"
      },
      {
        "command": "aiVoiceStudio.clearApiKey",
        "title": "Clear API Key…",
        "category": "AI Voice Studio"
      },
      {
        "command": "aiVoiceStudio.focusView",
        "title": "Focus Sidebar",
        "category": "AI Voice Studio"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "aiVoiceStudio.quickRead",
          "when": "editorHasSelection",
          "group": "1_modification@99"
        }
      ]
    },
    "keybindings": [
      {
        "command": "aiVoiceStudio.quickRead",
        "key": "ctrl+alt+r",
        "mac": "cmd+alt+r",
        "when": "editorTextFocus"
      },
      {
        "command": "aiVoiceStudio.stop",
        "key": "ctrl+alt+s",
        "mac": "cmd+alt+s"
      }
    ],
    "configuration": {
      "title": "AI Voice Studio",
      "properties": {
        "aiVoiceStudio.provider": {
          "type": "string",
          "enum": ["openai", "minimax", "mimo", "gemini"],
          "default": "openai",
          "description": "Active TTS provider."
        },
        "aiVoiceStudio.playbackRate": {
          "type": "number",
          "minimum": 0.5,
          "maximum": 4,
          "default": 1,
          "description": "Playback rate (0.5–4.0). Adjusts player speed without re-synthesis."
        },
        "aiVoiceStudio.chunkSize": {
          "type": "number",
          "minimum": 80,
          "maximum": 2000,
          "default": 250,
          "description": "Maximum characters per synthesis chunk. Long text is split at sentence boundaries; the next chunk synthesizes while the current one plays."
        },
        "aiVoiceStudio.openai.model": {
          "type": "string",
          "enum": ["gpt-4o-mini-tts", "tts-1", "tts-1-hd"],
          "enumDescriptions": [
            "GPT-4o Mini TTS — supports speaking instructions",
            "TTS-1 — legacy model, faster",
            "TTS-1 HD — legacy model, higher quality"
          ],
          "default": "gpt-4o-mini-tts",
          "description": "OpenAI text-to-speech model."
        },
        "aiVoiceStudio.openai.voice": {
          "type": "string",
          "default": "cedar",
          "description": "Default OpenAI voice."
        },
        "aiVoiceStudio.openai.format": {
          "type": "string",
          "enum": ["mp3", "opus", "aac", "flac", "wav", "pcm"],
          "enumDescriptions": [
            "MP3 — general-purpose (default)",
            "Opus — low-latency internet streaming",
            "AAC — iOS / Android / YouTube",
            "FLAC — lossless archival",
            "WAV — uncompressed, low-latency playback",
            "PCM — raw 24 kHz 16-bit samples, no header"
          ],
          "default": "mp3",
          "description": "OpenAI audio response format."
        },
        "aiVoiceStudio.openai.instructions": {
          "type": "string",
          "default": "",
          "description": "Speaking instructions (gpt-4o-mini-tts only). Example: 'Read naturally; preserve Chinese and English pronunciation.'"
        },
        "aiVoiceStudio.openai.speed": {
          "type": "number",
          "minimum": 0.25,
          "maximum": 4,
          "default": 1,
          "description": "OpenAI server-side synthesis speed (0.25–4.0). 1.0 is the default."
        },
        "aiVoiceStudio.openai.baseUrl": {
          "type": "string",
          "default": "https://api.openai.com/v1",
          "description": "OpenAI-compatible base URL."
        },
        "aiVoiceStudio.minimax.model": {
          "type": "string",
          "enum": [
            "speech-2.8-hd",
            "speech-2.8-turbo",
            "speech-2.6-hd",
            "speech-2.6-turbo",
            "speech-02-hd",
            "speech-02-turbo",
            "speech-01-hd",
            "speech-01-turbo"
          ],
          "enumDescriptions": [
            "Speech 2.8 HD — emotion fused with 语气词 (laughs/sighs/etc).",
            "Speech 2.8 Turbo — fastest 2.8 generation.",
            "Speech 2.6 HD — low-latency, normalized prosody.",
            "Speech 2.6 Turbo — best for chat / digital human.",
            "Speech 02 HD — premium prosody and clone fidelity.",
            "Speech 02 Turbo — strong on small languages.",
            "Speech 01 HD — legacy stable.",
            "Speech 01 Turbo — legacy fast."
          ],
          "default": "speech-2.8-hd",
          "description": "MiniMax T2A model. The speech-2.8 family also supports inline 语气词 like (laughs)/(sighs)."
        },
        "aiVoiceStudio.minimax.voice": {
          "type": "string",
          "default": "Chinese (Mandarin)_Radio_Host",
          "description": "Default MiniMax voice ID."
        },
        "aiVoiceStudio.minimax.format": {
          "type": "string",
          "enum": ["mp3", "wav"],
          "default": "mp3",
          "description": "MiniMax audio output format."
        },
        "aiVoiceStudio.minimax.region": {
          "type": "string",
          "enum": ["mainland", "global"],
          "default": "mainland",
          "description": "MiniMax API region (mainland: api.minimaxi.com, global: api.minimax.io)."
        },
        "aiVoiceStudio.minimax.speed": {
          "type": "number",
          "minimum": 0.5,
          "maximum": 2,
          "default": 1,
          "description": "MiniMax synthesis speed (0.5–2.0). Server-side; affects pacing/prosody."
        },
        "aiVoiceStudio.minimax.sampleRate": {
          "type": "number",
          "default": 32000,
          "description": "MiniMax audio sample rate (Hz)."
        },
        "aiVoiceStudio.minimax.bitrate": {
          "type": "number",
          "default": 128000,
          "description": "MiniMax audio bitrate."
        },
        "aiVoiceStudio.minimax.languageBoost": {
          "type": "string",
          "default": "",
          "description": "MiniMax language boost hint (e.g. 'Chinese' or 'English' or 'auto'). Optional. 40+ languages supported — see docs."
        },
        "aiVoiceStudio.minimax.vol": {
          "type": "number",
          "minimum": 0,
          "maximum": 10,
          "default": 1,
          "description": "MiniMax output volume (0–10). 1 is the doc default; bump up only if rendered audio is too quiet."
        },
        "aiVoiceStudio.minimax.pitch": {
          "type": "number",
          "minimum": -12,
          "maximum": 12,
          "default": 0,
          "description": "MiniMax pitch shift in semitones (-12 to +12). 0 keeps the voice natural."
        },
        "aiVoiceStudio.minimax.emotion": {
          "type": "string",
          "enum": ["auto", "happy", "sad", "angry", "fearful", "disgusted", "surprised", "neutral"],
          "default": "auto",
          "description": "MiniMax synthesis emotion. 'auto' lets the model decide. The speech-2.8 family fuses this with 语气词 markers."
        },
        "aiVoiceStudio.minimax.channel": {
          "type": "number",
          "enum": [1, 2],
          "default": 1,
          "description": "Output channel: 1 = mono, 2 = stereo."
        },
        "aiVoiceStudio.minimax.pronunciationDict": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "MiniMax pronunciation overrides. One entry per item, e.g. '处理/(chu3)(li3)' (pinyin) or '危险/dangerous' (replacement)."
        },
        "aiVoiceStudio.mimo.model": {
          "type": "string",
          "enum": [
            "mimo-v2.5-tts",
            "mimo-v2.5-tts-voicedesign",
            "mimo-v2.5-tts-voiceclone",
            "mimo-v2-tts"
          ],
          "enumDescriptions": [
            "Preset voices (Bingtang / Chloe / etc.). Supports singing mode and tag control.",
            "Voice Design — describe the voice in plain language; no preset needed.",
            "Voice Clone — clone a custom voice from an mp3/wav sample (≤10 MB).",
            "Legacy V2 model with the original Chinese / English presets."
          ],
          "default": "mimo-v2.5-tts",
          "description": "MiMo TTS model. v2.5 adds Voice Design and Voice Clone alongside the preset model."
        },
        "aiVoiceStudio.mimo.voice": {
          "type": "string",
          "default": "mimo_default",
          "description": "Default MiMo voice ID. Ignored for the voicedesign / voiceclone models."
        },
        "aiVoiceStudio.mimo.format": {
          "type": "string",
          "enum": ["mp3", "wav"],
          "default": "wav",
          "description": "MiMo audio output format."
        },
        "aiVoiceStudio.mimo.baseUrl": {
          "type": "string",
          "default": "https://token-plan-cn.xiaomimimo.com/v1",
          "description": "MiMo Token Plan base URL. Same host serves the OpenAI-compatible /v1 (TTS) and Anthropic-compatible /anthropic routes; both share Token Plan billing."
        },
        "aiVoiceStudio.mimo.stylePrompt": {
          "type": "string",
          "default": "",
          "description": "Style instruction sent as the role:user message. Doubles as the voice description for the voicedesign model (required there). Use the Insert Director Mode template button in the sidebar for richer control."
        },
        "aiVoiceStudio.mimo.openingStyleTags": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Opening-style tags injected as a leading (tag) prefix on the synthesized text (e.g. '开心', '严肃', '兴奋', '粤语', '夹子音'). Custom values are accepted in addition to the doc presets. The 唱歌 tag overrides everything else."
        },
        "aiVoiceStudio.mimo.audioEventTags": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Audio-event tags injected as a Chinese-bracket prefix (e.g. '紧张', '笑', '深呼吸', '哽咽'). Custom values are accepted alongside the doc presets."
        },
        "aiVoiceStudio.mimo.stylePresets": {
          "type": "array",
          "default": [],
          "description": "Saved (name, stylePrompt, opening tags, event tags) bundles. Manage them from the sidebar's Preset row.",
          "items": {
            "type": "object",
            "required": ["name"],
            "properties": {
              "name": { "type": "string" },
              "stylePrompt": { "type": "string" },
              "openingStyleTags": { "type": "array", "items": { "type": "string" } },
              "audioEventTags": { "type": "array", "items": { "type": "string" } }
            }
          }
        },
        "aiVoiceStudio.gemini.model": {
          "type": "string",
          "enum": [
            "gemini-3.1-flash-tts-preview",
            "gemini-2.5-flash-preview-tts",
            "gemini-2.5-pro-preview-tts"
          ],
          "enumDescriptions": [
            "Latest preview. 30 prebuilt voices, free on AI Studio free tier.",
            "Earlier Flash TTS preview, same 30-voice roster.",
            "Pro variant of the 2.5 TTS preview, higher quality."
          ],
          "default": "gemini-3.1-flash-tts-preview",
          "description": "Google Gemini speech-generation model."
        },
        "aiVoiceStudio.gemini.voice": {
          "type": "string",
          "default": "Kore",
          "description": "Default Gemini prebuilt voice. 30 personalities (Zephyr, Puck, Kore, Charon, …)."
        },
        "aiVoiceStudio.gemini.baseUrl": {
          "type": "string",
          "default": "https://generativelanguage.googleapis.com/v1beta",
          "description": "Generative Language API base URL. The TTS endpoint is appended as /models/{model}:generateContent."
        },
        "aiVoiceStudio.gemini.stylePreamble": {
          "type": "string",
          "default": "",
          "description": "Optional inline preamble prefixed in front of each chunk (e.g. 'Read in a calm narrator voice'). Combine with [bracket] audio tags inside the text for finer control."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "tsc -p ./ --noEmit",
    "test": "npm run compile && node --test test/*.test.cjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.95.0",
    "typescript": "^5.6.3"
  }
}
